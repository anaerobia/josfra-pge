# GCTP library top-level Makefile

# Boilerplate include (from HDF-EOS2 configuration)
include $(top_srcdir)/config/include.am

# Subdirectories
if INSTALL_INCLUDE_CONDITIONAL
SUBDIRS=src include
else
SUBDIRS=src
endif

